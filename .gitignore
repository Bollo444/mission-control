# dependencies
/node_modules

# next.js build output
/.next/
/out/
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# env / secrets (never commit API keys)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# local screenshots / scratch
/shots/
/shots$1.png

# the shared-memory vault is personal and lives in $HOME, never in the repo
/MissionControlVault/

# Sentinel — local security-agent workspace (754 cyber skills + swarm runtime +
# its own keys). Personal & heavy; never publish to the repo.
/sentinel/
/secswarm/

# oh-my-claudecode operational state (sessions, handoffs, logs) — never publish
/.omc/

# agent tooling / session state (never commit)
.freebuff/
.ijfw/
.ua/
.pi/
